name: weather-currency-bot
version: 1.0.0

dependencies: []

channels:
  chat-api:
    channel: chat-api
    publish: auto

scripts:
  main:
    source: src/main.sc

tests: test/test.xml